This document is for high-level understanding only. The source of truth is the code.

# Documentation

Humanaity is a monorepo: Spring Boot API (`apps/backend`), Angular UI (`apps/ui`), MCP server (`apps/mcp`). For setup and features, start with the [root README](../README.md).

**Active files here:** [`milestones.md`](milestones.md) (delivery tracker), [`dev-log.md`](dev-log.md) (dated notes). Older concept/spec markdown was moved to [`archive/2025-03-docs-consolidation/`](archive/2025-03-docs-consolidation/) for history only.

Agents: read source and tests before relying on any `.md` file (`agent-code-source-of-truth.mdc`).
